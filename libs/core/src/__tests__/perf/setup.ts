/**
 * Jest setup file for performance tests
 *
 * Note: Metrics are cleared by the reporter after results are written.
 * We don't clear them here because tests may run across multiple files
 * and we want to collect all metrics.
 */

// No-op - metrics are managed by the reporter
